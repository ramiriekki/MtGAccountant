export class Search{
    // name?: string
    // rarities?: string[]
    // minPrice?: number
    // maxPrice?: number
    // sets?: string[]
    // owned?: boolean

    constructor(
        public name?: string,
        public rarities?: string[],
        public minPrice?: number,
        public maxPrice?: number,
        public sets?: string[],
        public owned?: boolean
      ) {  }
}